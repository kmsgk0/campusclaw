# Generate with: python3 -c 'import secrets; print(secrets.token_urlsafe(24))'
# Set before first start. Existing users keep their stored password hashes.
SEED_PASSWORD=
COOKIE_SECURE=false
HOST_PORT=8080
BIND_ADDRESS=127.0.0.1
