# CampusClaw

无规约不写代码。行为变更先修改活动 change，再实现并运行对应 verify。
本项目是互联网软件开发课程第二、三课的个人实现。范围为登录、角色权限、班级隔离、教学材料上传下载与正文入库。
服务端身份决定角色与班级；禁止接受客户端指定的班级替代身份。数据库与上传目录不进入 Git。
验收必须覆盖实际浏览器操作与未登录、学生上传、跨班访问三条失败路径。真实会话凭证不写入作业证据。
